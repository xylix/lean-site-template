language: ruby
os: linux

# Clean and don't fail
install:
  - rm -rf public

# Build the website
script:
  - ./exec/hugo
  - cp CNAME public/

# Deploy to GitHub pages
deploy:
  provider: pages
  local_dir: public
  token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  on:
    branch: master
